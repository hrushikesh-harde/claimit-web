services:
  frontend:
    build:
      context: .
      args:
        # API URL used during frontend build
        - VITE_API_BASE_URL=${VITE_API_BASE_URL:-http://localhost:8080}

    container_name: claimit-frontend

    ports:
      # Host 5173 maps to Vite preview port 4173
      - "5173:4173"